/**
 *
 *? Tulis sebuah logika kode di dalam fungsi / method untuk menjumlahkan angka yang ada di dalam array
 *
 *! Contoh :
 ** [1, 2, 3, 4, 5] => 15
 ** [] => 0
 *
 */

/**
 *
 * @param {number[]} arr - argument yang menerima array
 * @returns {number}
 *
 */

const findAverage = (arr) => {
  // Tulis kode logika disini
};

console.log(findAverage([1, 2, 3, 4, 5])); // Expected = 15
console.log(findAverage([])); // Expected = 0
