/**
 *
 *? Tulis sebuah logika kode di dalam fungsi / method untuk mengkonversikan sebuah String menjadi sebuah Array
 *
 *! Contoh:
 ** "Hello World" => ["Hello", "World"]
 *
 */

/**
 *
 * @param {string} str argument yang menerima string
 * @returns {string[]}
 *
 */

const stringToArray = (str) => {
  // Tulis kode logika disini
};

console.log(stringToArray("Hello World!")); // Expected = ["Hello", "World!"]
